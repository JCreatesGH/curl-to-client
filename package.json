{
  "name": "curl-to-client",
  "version": "0.1.0",
  "description": "Turn a cURL command into a typed TypeScript fetch client, with response types inferred from a sample.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "test": "vitest run", "build": "tsc -p tsconfig.json" },
  "keywords": ["curl", "codegen", "typescript", "fetch", "api-client", "types"],
  "author": "Joshua John <josh202@gmail.com>",
  "license": "MIT",
  "devDependencies": { "vitest": "^4.0.0", "typescript": "^5.4.0", "@types/node": "^20.0.0" }
}
